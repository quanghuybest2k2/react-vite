version: '3.8'
services:
    client:
        container_name: react-vite
        build:
            context: .
            dockerfile: ./docker/Dockerfile
        ports:
            - '80:80'
        environment:
            - TZ=Asia/Ho_Chi_Minh
